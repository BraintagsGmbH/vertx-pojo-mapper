# vertx-pojo-mapper-json

This module contains common implementations of interfaces of pojo mapper for all implementations which are using JSon as
format to persist Java objects into the IDataStore
   
    
